<div class="container">
  <h1><%= @subject.name %></h1>
  <%= image_tag "#{@subject.img}" %>
  <p> <%=@subject.desc%></p>
  <br>
  <%= link_to "Edit Subject", edit_subject_path %>
  -
  <%= link_to "Delete Subject", @subject, method: :delete %>

  <%= render partial: 'topics/topics', collection: @subject.topics, as: :topic %>
</div>
